<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.soft.dc.recordengine30">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/icon_buum_tv"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_buum_tv_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AudioControllerActivity" />
        <activity android:name=".DevActivity" />
        <activity android:name=".MediaActivity" />
        <activity android:name=".utube.YoutuberecordActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ChooseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>